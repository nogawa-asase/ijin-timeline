# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

---

## フェーズ1: 実装

- [x] `src/ui/person-input.js` を変更する
  - [x] 定数 `SHORT_QUERY_LENGTH` と `MESSAGE_NOT_FOUND_SHORT_QUERY` を追加する
  - [x] 関数 `notFoundMessageOf(query)` を追加する
  - [x] `showCandidates` に `query` を渡し、0件のときに `notFoundMessageOf(query)` を表示する(呼び出し元2か所)

## フェーズ2: 永続ドキュメントの更新

- [x] `docs/product-requirements.md` を更新する
  - [x] 機能1の受け入れ条件に、2文字以下で0件のときの案内を追記する
  - [x] ユーザビリティの「エラーメッセージ」の例に追記する
- [x] `docs/functional-design.md` のエラーハンドリングの表に、短い入力で0件の行を追加する
- [x] `docs/glossary.md` の画面の文言の表に追記する
- [x] `docs/manual-test-checklist.md` に「紫」「紫式」の確認項目を追加する

## フェーズ3: 品質チェックと修正

- [x] すべてのユニットテストが通ることを確認する
  - [x] `node --test 'tests/**/*.test.js'`(111件すべて成功)
- [x] 実データで「紫」が0件、「紫式」で紫式部が出ることを確認する(Node.jsから実際のAPIを呼ぶ。「あいうえおかきくけこ」は0件、「織田」は7件)
- [x] ブラウザで手動テストする(ユーザーに依頼。キャッシュを無効化して再読み込み。2026-09-25 ユーザー確認済み)
  - [x] 「紫」で「該当する人物が見つかりませんでした。名前をもう少し長く入力してみてください」
  - [x] 「紫式」で紫式部が候補に出る
  - [x] 「あいうえおかきくけこ」で「該当する人物が見つかりませんでした」
  - [x] 「織田」で候補が表示される

## フェーズ4: 振り返り

- [x] 実装後の振り返り（このファイルの下部に記録）

---

## 実装後の振り返り

### 実装完了日
2026-09-25

### 計画と実績の差分

**計画と異なった点**:
- なし。計画のレビューで、メッセージを「案内だけ」にする案・案内を出さない案と比べ、結果と次の行動の両方を伝える計画どおりの文言に決めた

**新たに必要になったタスク**:
- なし

**進め方の差分**:
- 手動テストの結果の報告より先にコミットの指示があったため、実装とドキュメントを先にコミットし(`20de7f0`)、手動テストの確認後にtasklistと振り返りを追加でコミットした

### 学んだこと

**技術的な学び**:
- `wbsearchentities` は1文字の検索では一般的な語(色・物質・地名など)が上位を占め、人物が上位20件に入らないことがある。「紫」では紫式部が22位だった
- 取得件数を増やすより、利用者に次の行動を案内するほうが、通信量を増やさずに多くの検索語に効く

**プロセス上の改善点**:
- 原因を実データで調べ(検索順位の確認)、対策の選択肢を通信量・効果で比べてから決めたため、方針の判断が早かった

### 次回への改善提案
- 手動テストの結果を確認してからコミットする順番にそろえると、tasklistの記録を1回のコミットにまとめられる
- 画面のメッセージの出し分けが増えてきたら、メッセージを選ぶ関数を純粋関数として切り出し、ユニットテストの対象にすることを検討する
