# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### 実装可能なタスクのみを計画
- 計画段階で「実装可能なタスク」のみをリストアップ
- 「将来やるかもしれないタスク」は含めない
- 「検討中のタスク」は含めない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

### タスクが大きすぎる場合
- タスクを小さなサブタスクに分割
- 分割したサブタスクをこのファイルに追加
- サブタスクを1つずつ完了させる

---

## フェーズ1: ロジックレイヤー

- [x] `src/logic/years.js` を実装する(変換・年数・表記・代表年・生没年の表記)
- [x] `tests/unit/logic/years.test.js` を作成する
- [x] `src/logic/comparison.js` を実装する(A5)
- [x] `tests/unit/logic/comparison.test.js` を作成する
- [x] `src/logic/timeline-scale.js` を実装する(A6)
- [x] `tests/unit/logic/timeline-scale.test.js` を作成する

## フェーズ2: データレイヤー

- [x] Wikidataの実際の応答からフィクスチャを作成する(`tests/fixtures/`)
- [x] `src/data/person-parser.js` を実装する(A1, A2)
- [x] `tests/unit/data/person-parser.test.js` を作成する
- [x] `src/data/wikidata-client.js` を実装する(検索・詳細取得・タイムアウト・中断)
- [x] `tests/unit/data/wikidata-client.test.js` を作成する

## フェーズ3: UIレイヤー

- [x] `index.html` と `.nojekyll` を作成する(CSP、画面の骨組み、出典表示)
- [x] `css/style.css` を作成する(レスポンシブ、600px切り替え、タップ対象44px)
- [x] `src/ui/result-view.js` を実装する
- [x] `src/ui/timeline-view.js` を実装する
- [x] `src/ui/person-input.js` を実装する(combobox、デバウンス、中断、キーボード操作)
- [x] `src/app.js` を実装する(AppState、再描画、resize)
- [x] 開発サーバーで静的ファイルが配信され、実APIで検索〜比較の流れが動くことをNode.jsから確認する

## フェーズ4: 品質チェックと修正

- [x] すべてのユニットテストが通ることを確認する(`node --test 'tests/**/*.test.js'`)
- [x] 全JavaScriptファイルの構文チェックが通ることを確認する(`node --check`)
- [x] 規約チェック(`innerHTML` 不使用、`export default` 不使用、依存関係ルール、ファイル300行以下)をgrepで確認する(timeline-view.js 354行・person-input.js 303行・style.css 318行が目安超過。振り返り参照)
- [x] 静的ファイルの合計が100KB以内(58.7KB)であることを確認する
- [x] `src/ui/timeline-view.js`(354行)を描画要素ごとに `src/ui/timeline/` へ分割し、300行以下にする
- [x] `src/ui/person-input.js`(303行)の選択・クリア・外部設定で重複している状態リセット処理を1つにまとめ、300行以下にする
- [x] `docs/repository-structure.md` に、分割したサブディレクトリ(`src/ui/timeline/`)の依存ルールを追記する
- [x] ユニットテスト・構文チェック・規約チェックを再実行する
- [x] `implementation-validator` サブエージェントで実装とスペックの整合性を検証し、指摘を反映する(上限で中断した検証のやり直し。必須0件、推奨2件・提案3件を反映、提案1件はPRDの今後の検討事項へ)

## フェーズ5: ドキュメント・後片付け

- [x] `docs/manual-test-checklist.md` を作成する(受け入れ条件、検証用20人とWikidataの生没年)
- [x] テンプレート由来のファイルを削除し `.gitignore` を整理する(`docs/repository-structure.md` の「テンプレートから削除するファイル」)
- [x] `README.md` を更新する
- [x] 機能設計書などの永続ドキュメントに実装時の変更点を反映する
- [x] 実装後の振り返り(このファイルの下部に記録)

---

## 実装後の振り返り

### 実装完了日
2026-09-24

### 計画と実績の差分

- **作業の中断と再開**: `claude -p` による自動実行が、フェーズ5の途中(検証サブエージェントの実行中)で利用上限に達して停止した。成果物を確認したうえで、残りの作業(ファイル分割・検証のやり直し・振り返り)を対話セッションで引き継いだ
- **ファイルの追加**: 表示範囲・目盛りの計算(A6)をユニットテストできるよう、`src/logic/timeline-scale.js` を追加した(機能設計書の当初のコンポーネント一覧にはなかった)
- **インターフェースの変更**:
  - `searchPeople(query, currentYear, signal)`: データレイヤーで現在時刻を取得しないため、存命判定用の `currentYear` を引数に追加した
  - `renderTimeline(svg, slots, ...)`: 線の色を入力欄の番号で決めるため、`people` ではなく `AppState.slots` を受け取るようにした
  - `Comparison` の重なり年を `YearValue` で持つようにした(文章で「970年代」のような精度付きの表記をするため)
- **表示名のフォールバック**: Wikidataの全言語共通ラベル(`mul`)を、日本語と英語の間に追加した。PRD・機能設計書・用語集に反映済み
- **ファイル分割**: `timeline-view.js`(354行)を `src/ui/timeline/person-row.js` と `svg.js` に分割して131行に、`person-input.js`(303行)は重複していた状態リセット処理を `applySelection` にまとめて298行にした。`src/ui/` 内の import 禁止ルールとぶつかるため、「部品を分割したサブディレクトリは、その部品からのみ import してよい」とリポジトリ構造定義書に追記した
- **テンプレートの後片付け**: 「MVP完了後の最後の作業」としていたテンプレート由来ファイルの削除を、本作業のフェーズ5で実施した
- **検証で見つかった仕様の穴**: 「先の人物が亡くなった年に後の人物が生まれた」場合は、A5の判定で必ず「重なり0年」になり、「空白期間0年」の文章は表示されないことが分かった。実装の到達しない分岐を削除し、PRD・機能設計書を修正した
- **未解決**: `css/style.css` が318行で目安(300行)を超えている。同じ人物を両方の入力欄で選んだ場合の扱いは、PRDの今後の検討事項に記録した

### 学んだこと

- DOMに依存しない計算(目盛り・表示範囲・比較)をロジックレイヤーに寄せると、ブラウザなしでもNode.jsの組み込みテストで大半を検証できる(90件)
- Wikidataの実APIでの確認(信長と家康、孔子、リンカーンとダーウィン、ガリレオとニュートン)は、境界値の仕様の穴(空白期間0年は起こらない)を見つけるのに有効だった
- 天文学的年と歴史的年を変数名(`astroYear` / `year`)で区別する規約が、紀元前をまたぐ計算の取り違えを防いだ
- 長時間の自動実行は利用上限で途中停止しうる。`tasklist.md` に進捗が残っていたため、どこから再開すればよいかがすぐ分かった

### 次回への改善提案

- 自動実行(`claude -p`)は利用枠に余裕があるときに始め、フェーズごとにコミットして中断に備える
- 結果の文章を組み立てる処理(`result-view.js` の `buildSentences`)はDOMに依存しない純粋関数なので、`src/logic/` に移してユニットテストの対象にすることを検討する
- ブラウザでの手動テスト(`docs/manual-test-checklist.md`)を、公開前に実施する
- `css/style.css` を画面の部品ごとに分割するかを判断する
